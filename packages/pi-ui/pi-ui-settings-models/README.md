# @workbench/pi-ui-settings-models

Owns Pi provider and model configuration UI, draft normalization, credentials links, autosave, and test feedback. Pi runtime and protocol dependencies remain in the Pi domain.
