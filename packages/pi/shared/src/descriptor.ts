import { defineWorkbenchAgentRuntimeDescriptor } from "@workbench/agent-runtime-contracts/descriptor";

/** The shared identity of the only Agent Runtime currently installed by Workbench. */
export const PI_AGENT_RUNTIME_DESCRIPTOR = defineWorkbenchAgentRuntimeDescriptor("pi");
