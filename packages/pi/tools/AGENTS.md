# Pi inline tools

Preserve tool IDs, schemas, output budgets and extension installation order. Use SDK public truncateHead/truncateTail and existing settings subscriptions. Business factories and Todo state live in src; consumed helpers live in lib; TS/TSX only, maximum one directory level each. Do not import root server, registries or StreamHub. Receive host behavior via WorkbenchToolDependencies and the server ports; trace remains the final observer.
