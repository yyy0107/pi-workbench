# @workbench/ui-sidebar

Owns generic sidebar providers, row/group primitives, pointer drag sessions and regional styles. Conversation navigation lives in ui-conversation-list, which consumes public primitives/reorder/order entries. src/sidebar-reorder.ts exposes the actual generic algorithms in lib/sidebar-reorder.ts. Tests live at package root.
