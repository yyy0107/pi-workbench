# @workbench/ui-disclosure

Owns shared disclosure layout compensation for expanding conversation content. It provides the scroll-direction context, compensated details component, shared scroll lock and unlock event, lock query, and the pure compensation policy in `lib`.

The viewport and disclosure components must import this package so they observe the same module-level lock state. The package adds no product copy or translation bundle.
