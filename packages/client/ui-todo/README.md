# @workbench/ui-todo

Owns Todo result adaptation, task list presentation, composer panel, tool renderer, and extension registration. The `./model` entry exposes the shared Todo recognition model without depending on the conversation package.
