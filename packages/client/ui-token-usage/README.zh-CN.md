# @workbench/ui-token-usage

Owns token usage, context statistics, animation helpers, and the token usage extension. UI tests remain colocated but are excluded from migration validation.

负责 token 用量、上下文统计、动画 helper 和 token 用量扩展。UI 测试仍与能力共置，但不纳入迁移验证。
