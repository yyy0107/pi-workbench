# @workbench/ui-side-chat

Owns side-chat UI, its extension, local dictionaries and consumed helpers. Source remains shallow TypeScript; tests live at the package root. Consume public exports. Shell installs the extension in the original order.

本包拥有 side-chat 界面、扩展、双语词典与被消费的辅助逻辑。保持原 ID、Portal、状态与释放次序；UI测试在Spec004中保留但不执行。
