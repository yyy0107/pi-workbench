# @workbench/ui-settings-general

Contributes locale and conversation preference settings to the existing settings container. It does not own the settings registry, main view, or sidebar container.

向现有 settings 容器贡献语言和会话偏好设置项。不拥有 settings registry、主视图或侧栏容器。
