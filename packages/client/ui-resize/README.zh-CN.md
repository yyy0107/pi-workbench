# @workbench/ui-resize

负责 Workbench 共享尺寸调整能力：可折叠与比例尺寸 Hook、无障碍调整手柄、弹簧与吸附计算、尺寸观察以及比例面板样式。`src` 存放公开 React 集成和 CSS，`lib` 存放被实际消费且不依赖框架的计算与观察辅助模块。

本包保持既有指针、键盘、减少动态效果、Observer、动画和清理行为。可见文案与无障碍标签由调用方传入；本包不拥有产品文案或翻译 bundle。
