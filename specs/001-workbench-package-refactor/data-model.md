# 重构管理对象

没有新增业务数据实体或持久化迁移。

- 能力包：唯一 package name、领域/能力根、职责、exports、生产/开发依赖和测试目录；实际来源唯一，生产依赖无环。
- 迁移任务：唯一 T 编号、前置编号、路径范围、消费者、验证条件；pending → implemented → verified。只有 verified 可勾选，失败保留未完成。
- 过渡违规：规则、包名、来源路径和目标；只允许现有精确条目，不允许按目录通配豁免；最终删除。
